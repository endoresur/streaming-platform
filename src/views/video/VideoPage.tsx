import { NextPage } from 'next'

import styles from './styles.module.scss'

const VideoPage: NextPage = () => {
  return <section className={styles.videoPageRoot}>video page</section>
}

export default VideoPage
